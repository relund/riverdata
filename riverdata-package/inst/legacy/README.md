# Legacy R code

These files preserve the original camelCase implementations for reference.
They are not loaded, exported, or used by the `riverdata` package.

Use the snake_case functions under `R/` for active development.
